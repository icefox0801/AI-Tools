# Environment Configuration Example
# This file is for local development reference only.
# For Docker, configure via docker-compose.yaml environment section.
#
# Note: Most settings are now hardcoded in services for simplicity.
# Only the following are configurable:

# Text Refiner Service
# Enable/disable ASR error correction
ENABLE_CORRECTION=true

# Audio Notes App (local development)
# These only apply when running outside Docker
WHISPER_URL=http://localhost:8003
PARAKEET_URL=http://localhost:8002
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:14b
